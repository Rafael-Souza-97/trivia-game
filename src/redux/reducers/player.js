const initalState = {
  name: '',
  assertions: 0,
  score: 0,
  gravatarEmail: '',
};

function playerReducer(state = initalState, action) {
  switch (action.type) {
  default:
    return state;
  }
}

export default playerReducer;
